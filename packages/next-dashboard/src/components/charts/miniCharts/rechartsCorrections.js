// @flow

// Picharts render a few pixels below the center and in a different size than radial barcharts
// ... why? Ask recharts.
const RENDER_ISSUE_OFFSET_PADDING = 11;
const RENDER_ISSUE_OFFSET_SIZE = 11;

export { RENDER_ISSUE_OFFSET_PADDING, RENDER_ISSUE_OFFSET_SIZE };
