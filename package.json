{
  "name": "next-dashboard-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "> TODO: description",
  "author": "Linn Dahlgren <linn.dahlgren@pija.se>",
  "homepage": "https://github.com/PiJaAB/next-dashboard#readme",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "node scripts/pnpm-select.js dev",
    "build": "pnpm --silent -C ./package build",
    "lint": "pnpm --silent -C ./package lint",
    "deploy": "node scripts/deploy.mjs"
  },
  "pnpm": {
    "overrides": {
      "@pija-ab/next-dashboard": "workspace:^"
    }
  },
  "devDependencies": {
    "execa": "^7.1.1",
    "js-yaml": "^4.1.0",
    "semver": "^7.5.4"
  }
}
